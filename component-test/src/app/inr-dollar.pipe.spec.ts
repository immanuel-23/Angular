import { InrDollarPipe } from './inr-dollar.pipe';

describe('InrDollarPipe', () => {
  it('create an instance', () => {
    const pipe = new InrDollarPipe();
    expect(pipe).toBeTruthy();
  });
});
