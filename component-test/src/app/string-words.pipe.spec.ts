import { StringWordsPipe } from './string-words.pipe';

describe('StringWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new StringWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
